# 入门指南

此应用程序中的所有工具均按所属工具集分组列在左侧侧边栏中。选择其中一个工具，它将作为一个新标签页打开。

您可以同时打开多个工具，每个工具都是独立运行的——在一个标签页中启动耗时较长的任务不会阻塞其他标签页。关闭标签页**不会**停止仍在运行的工具：它会在后台完成并在历史记录中正常记录。

顶部的药丸形状按钮行可按标签筛选侧边栏。选择多个标签会将列表缩小为包含*所有*所选标签的分组，而不是包含任一标签的分组。点击 ``全部``（``All``）可清除筛选。

侧边栏上方的搜索框可按工具名称进行筛选。
