# 入門指南

此應用程式中的所有工具均按所屬工具集分組列在左側側邊欄中。選擇其中一個工具，它將作為一個新分頁開啟。

您可以同時開啟多個工具，每個工具都是獨立執行的——在一個分頁中啟動耗時較長的工作不會阻礙其他分頁。關閉分頁**不會**停止仍在執行的工具：它會在背景完成並在歷史記錄中正常記錄。

頂部的膠囊形狀按鈕列可按標籤篩選側邊欄。選擇多個標籤會將清單縮小為包含*所有*所選標籤的分組，而不是包含任一標籤的分組。點擊 ``全部``（``All``）可清除篩選。

側邊欄上方的搜尋框可按工具名稱進行篩選。
